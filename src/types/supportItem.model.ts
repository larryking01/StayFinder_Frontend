// define the shape of a support item
import type { LucideIcon } from "lucide-react"

export interface SupportItem {
    name: string,
    icon: LucideIcon    // ensure only lucide icons can be used here
}